# Nathan — C07 checkpoint feedback — 2026-08-16

**Graded at commit `fd66b41` ("update for C07") · branch `checkpoint-c07` · date 2026-08-16**

**Policy: accept-all.** This grades the artefact exactly as submitted. Building on the
studied projects and using AI help are both legitimate and neither discounts a verdict.
No ownership or authenticity cap is applied here. Authenticity is tested once, live in
Part B of the Live Coding Validation, where every claim below has to be defended.

**Rubric:** the four reference pages in `C07-Reference-Godot/`. Each code's *"Earns the
tick"* line is the standard; where my verdict differs from the checkpoint-2 round, the
page won and I have said so explicitly in **Where this round grades differently**.

**How to use this branch.** Do not merge it. Read the doc, make every fix on `main` in
your own commits. This branch grades **C7 codes only** — but see the cross-criterion
warning below, because this round did something to your C6 labels you need to know about.

---

## Score

| | |
|---|---|
| Label claims found | **47** (permissive sweep: `# C7`, `#C7`, `##C7`, spaced/unspaced, any suffix, inline) |
| Distinct codes touched | **21 of 21** — every code on the C7 tick sheet is claimed at least once |
| Formal cells claimed | **26 / 63** (codes carrying a proper `a`/`b`/`c` suffix) |
| Malformed labels | **21** — every single feature-`a` label, none of which carries an `a` |
| **EARNED** | **33** |
| **SHORT** | **14** |
| **NOT FOUND** | **0** — every label points at code that actually exists |

**Feature map:** `a` = `Scripts/circular_motion.gd` + `Scripts/base_motion.gd` ·
`b` = `Scripts/HomeScreen.gd` · `c` = `Scripts/Settings.gd` + `Scripts/switch.gd`

| Feature | Claims | EARNED | SHORT | Formal cells (suffix correct) |
|---|---|---|---|---|
| a — circular motion | 21 | 14 | 7 | **0 / 21** — all malformed |
| b — home screen | 15 | 10 | 5 | 15 / 15 |
| c — settings | 11 | 9 | 2 | 11 / 11 |
| **Total** | **47** | **33** | **14** | **26 / 63** |

**The number that matters: 14 of your 33 EARNED verdicts sit on labels that cannot be
counted into a cell as written.** Every feature-`a` label reads `C711`, `C723`, `C744`
— no `a`. The checkpoint sheet has three boxes per code and a bare `C744` does not name
one. Adding one character to 21 comment lines converts fourteen confirmed cells from
uncountable to countable. Nothing else in this report is that cheap.

Zero NOT FOUND, third round running. You do not claim code you have not written.

---

## ⚠ Cross-criterion warning — this commit deleted 32 of your C6 labels

Out of C7 scope for verdicts, but you need to see it before you do anything else.

| | C6 labels in `.gd` files |
|---|---|
| `8d9d1c4` (the commit your C06 feedback graded) | **77** |
| `fd66b41` (this commit) | **45** |

The rewrite of `base_motion.gd` and `circular_motion.gd` replaced the old comment blocks
wholesale, and the C6 labels went with them. **20 of the 22 feature-`a` cells confirmed
EARNED in your C06 report are now unlabelled** — C611a, C612a, C613a, C614a, C615a,
C621a, C622a, C623a, C624a, C626a, C627a, C628a, C629a, C632a, C643a, C644a, C645a,
C651a, C654a, C655a. Only C631a and C634a survive, and only because they live in
`Global.gd`, which you did not touch.

The code is all still there. The evidence is not. `git show 8d9d1c4:Scripts/base_motion.gd`
and `git show 8d9d1c4:Scripts/circular_motion.gd` have every one of those labels; putting
them back is a copy job, not new work. Do it before the C6 validation.

---

## What changed since the last graded round

Checkpoint 2 was graded at `4f77411` on branch `checkpoint2`. Only two files changed
between there and here — `Scripts/base_motion.gd` and `Scripts/circular_motion.gd`.
Features b and c are byte-for-byte unchanged for the **third** round running.

### The headline: you rewrote feature a's documentation in your own voice

This is the right instinct and it is worth saying plainly. Last round's C7-2 problem was
that feature a had *nothing above identify level*. Now feature a claims all nine C7-2
codes, in plain first-person prose that reads like someone who understands the code:

> `bm:8` "kept all the shared stuff (validation, saving/loading, play/pause) in this base
> class so `circular_motion.gd` doesn't have to repeat it, if I add more simulations later
> they just inherit from here instead of copy pasting"

That is a genuine *explain* — a choice, plus the alternative rejected (copy-pasting).
It earns **C744**, and it is writing you can reproduce under exam conditions, which is
exactly what Part B tests. Six of the nine C7-2 cells in feature a now stand.

### Checked against last round's list

| Asked for at checkpoint 2 | Status |
|---|---|
| Retire the rejected `C741a` on the autogenerated handler name (`_on_button_3_button_down`) | ✅ **Done.** The label is gone, and `C741` now sits on `bm:25` over the `get_`/`set_` pattern with `class_name BaseMotionSimulation` above it. Confirmed EARNED. |
| Label `C741a` on `class_name BaseMotionSimulation` / `try_parse_validated_float` — "the cell you've held since last round, still unlabelled" | ✅ **Done.** Same line as above. |
| Claim the three free validation cells (`C723a`/`C724a`/`C725a`) | ✅ **Still claimed** at `bm:53`/`:57`/`:63`, rewritten in your own words. All three EARNED. (Suffix still missing — see below.) |
| Restore the deleted explain header (C742a/C743a/C744a) | ⚠ **Two of three.** `C744` and `C742` are back as real explains. `C743` came back as a *description*, not an explanation — see its verdict. |
| Fix `C731c` — `$Panel`/`$Panel2`/`$Sprite2D`/`$Sprite2D2` are Godot defaults | ❌ **Not done.** `switch.tscn` is unchanged; the four node names are still the defaults, and the comment still claims they "describe exactly which visual layer each node represents". |
| Retire or fix `C725b`/`C735b` (internal-state guards claimed as input validation) and `C724b` (event dispatch) | ❌ **Not done.** `HomeScreen.gd` is unchanged. All three still SHORT for the same reason as last round. |
| Do the scene-tree rename so `C751a`/`C751b`/`C751c` become claimable | ❌ **Not done.** All three scenes are unchanged. All three C751 claims still fail on the same evidence. |
| Name the inputs in `C713a` (including the imported CSV) | ⚠ **Half.** The text boxes and sliders are now named specifically — a real improvement on "User uses buttons, text boxes and sliders". The imported CSV file is still missing. |
| Write the reasonableness rule → `C753a` | ❌ **Not done.** The completeness check is labelled and correct; the reasonableness rule is not written anywhere. |
| Name the magic `30.0` velocity scale as a constant | ❌ **Not done.** Still bare at `cm:111`, `cm:140`, `cm:155`. |
| Fix "FUNCTIONALALITY" / "customizablility" | ✅ **Done.** Clarity qualifiers bite at the higher bands; this is the kind of pass that keeps them off you. |

### Two things that went backwards

1. **`C743` dropped a rung.** Last round the report pointed at prose you had *already
   written* at old `bm:88–93` — "why: every value that can come from free-text user input
   … unlike slider input, which is already pre-clamped" — and called it explain-level
   C743a, free to label. That paragraph was deleted this round. Its replacement at `bm:78`
   describes what happens instead of why. Verb ladder: 5–6, not 7–8.

2. **The reset button is still broken, third round.** `Scenes/CircularMotion.tscn:890`
   still carries a connection to `_on_button_2_button_down`, which exists in no `.gd` file
   in the project. Pressing that button raises a missing-method error, and
   `_reset_sliders_to_defaults()` (`cm:190`) still has **no caller anywhere** — which
   matters here because your `C734` evidence lives inside it.

### And one thing worth banking

The validation chain is **live**, not scenery. `CircularMotion.tscn:884–886` connect
`text_submitted` on `radiusLabel`, `velocityLabel` and `massLabel` to the three handlers
that call `try_parse_validated_float()`. That means C723/C724/C725/C735/C745 all sit on
code that a real user keystroke actually reaches. That is the single strongest C7-3 work
in the class and the reason feature a's validation cells hold.

---

## Where this round grades differently from checkpoint 2

One reversal the teacher should adjudicate, because it moves a mark.

**`C731b` is downgraded to SHORT.** Last round credited it on the strength of the
variable name `formula_label`. The grader brief's strictness rule is explicit and binding:
*"Naming is audited across the whole feature. One inconsistent corner (`studentName` beside
`student_id`, **a node still called `Button1`**) caps C7-1 below the claimed level — name
the corner."* And the C071 page's own C731 anti-pattern is *"Godot's defaults left in
place: `Button1`, `LineEdit`, `Label2`."* `HomeScreen.tscn` contains `Button2`, `Button3`,
`Button4`, `Button5`, `Label2`, `Panel2`, `ReferenceRect2`/`3`/`4`, and its root Control
node is literally named `Node2D`. C731's earn line is *"every control in the picked
feature's scene named this way — the audit will open your scene tree and look."*

This is not new information — it is the same scene tree that already sinks `C751b`. What
changes is that it now sinks `C731b` too, and by the same rule `C731` in feature a. That
makes C7-1 tell one coherent story instead of three contradictory ones: **your code names
are good; your scene tree is not; and every C7-1 code above 3–4 is blocked on the same
afternoon of renaming.** See Top 3, item 2.

---

## Verdicts

### Feature a — `circular_motion.gd` · `base_motion.gd`

Abbreviations: `cm` = `Scripts/circular_motion.gd`, `bm` = `Scripts/base_motion.gd`.
**Every label in this table is missing its `a` suffix** — noted once here rather than
fourteen times in the table.

| Code | Skill | Verdict | Student's line (file:line) | What's missing |
|---|---|---|---|---|
| C711 | identifies naming conventions | **EARNED** | `cm:3–4` "naming convention used through this whole thing: snake_case for vars/funcs, ALL_CAPS for constants, private stuff gets a leading _" | — but this is *shorter* than the version you had at `4f77411`, which also covered classes, nodes and signals. Open the Part B audit with the fuller sentence. |
| C712 | identifies functioning | **EARNED** | `cm:6–10` "this script runs the circular motion sim, sliders control mass, radius and velocity, ball spins around the pivot and the force label updates live, can also save/load the current setup to a csv file" | — true, specific, and the typos are fixed |
| C713 | identifies input data | **SHORT** | `cm:262–264` "the mass/radius/velocity text boxes are the only place someone can type literally anything… sliders cant go out of range on their own" | Standard: "**every** user-facing input of the picked feature named." The imported **CSV file** is a user-facing input — the user picks it in `ImportFileDialog` and you validate it at `bm:112–119`. One more line names it. |
| C721 | applies naming to variables | **SHORT** | `cm:15–16` "vars named after what they hold + what control they are" over the three `@onready` slider declarations at `cm:12–14` | Standard: "**magic numbers replaced by named constants.**" `30.0` is still bare at `cm:111`, `cm:140`, `cm:155` — flagged last round, unchanged. `const VELOCITY_SCALE := 30.0` closes it. (Also: `tween_toggle` at `cm:47` — "booleans as `is_`/`has_` questions".) |
| C722 | outlines functioning | **EARNED** ⚠ | `cm:51–53` "on load: load the file dialogs, connect slider signals, work out starting values off whatever the sliders default to, then refresh the labels so nothing shows 0 or blank when it first opens" | — it outlines a real flow accurately. But it is not a **header comment**: the page defines one as "meaningful comments at the top of a source code file — the file's name, purpose, author and date", earning the tick when "a reader knows what happens in this file **without scrolling**". This is line 51 of 274. Move it up beside C712 and add the filename/author/date line. |
| C723 | existence checking | **EARNED** | `bm:53–56` "an empty box counts as no input at all" → `var trimmed: String = text.strip_edges()` / `if trimmed.is_empty(): return false` | — `strip_edges()` first is exactly the page's idiom |
| C724 | type checking | **EARNED** | `bm:57–59` "has to actually parse as a number not just text" → `if not trimmed.is_valid_float(): return false` | — `is_valid_float()` is the page's named workhorse |
| C725 | range checking | **EARNED** | `bm:63–64` "range check so something like mass = -50 cant sneak through" → `return clamp(value, min_value, max_value)`, bounds named at `bm:17–22` | — the limits being named constants is C7-1 working for C7-3, exactly as the page asks |
| C731 | applies naming to interface controls | **SHORT** | `cm:27–28` "each label/slider name tells you what it shows without opening the scene tree" | The comment describes your **variable** names (that's C721). Standard: "every control in the picked feature's **scene** named this way — the audit will open your scene tree and look." `CircularMotion.tscn` holds `Panel2`, `Panel3`, `Label2`, `Label3`, `Label4`, `Button2`, `Button3`, `Button4`, `ColorRect3`, plus camelCase (`massSlider`, `forceLabel`) and lowercase (`layout`, `options`, `show`, `hide`, `save`, `import`) node names. |
| C732 | describes functionality | **EARNED** | `cm:92–94` "works out F = mv^2/r off whatever the sliders currently say and writes it to the force label… skips the divide if radius is 0 so it cant crash the simulation" | — accurate, and it names the guard |
| C733 | describes use of data | **EARNED** | `cm:162–164` "grabs whatever the sliders currently say and hands it to the base class to write out, this script doesnt care how the csv gets written, just what values go into it" | — a stronger site is sitting unlabelled at `bm:121–126`, the returned Dictionary; see Unclaimed evidence |
| C734 | evidence of code maintenance | **EARNED** ⚠ | `cm:191–192` "defaults now live as named constants above… instead of a plain [2, 3, 3] (before i changed it) array where you had to guess what each number meant" | — a real before/after, and `git diff 4f77411 fd66b41` proves it. **But** the function it documents, `_reset_sliders_to_defaults()`, has no caller anywhere: the button that used to call it (`CircularMotion.tscn:890`) points at `_on_button_2_button_down`, which no longer exists. |
| C735 | two checks working | **EARNED** | `bm:67–68` "runs the existence and type checks together before anything even gets to the range check below" → `try_parse_validated_float()` at `bm:66–72`, reached from `cm:267`/`:270`/`:273` via the `text_submitted` connections at `CircularMotion.tscn:884–886` | — |
| C741 | applies naming to code structures | **EARNED** | `bm:25–26` "functions like get_mass/set_mass follow the normal get_/set_ pattern so its obvious what they do without reading the body" — over `bm:1` `class_name BaseMotionSimulation` (PascalCase noun) and verb-named `try_parse_validated_float()`, `save_state_to_csv()`, `pause_simulation()`, `slow_down()` | — this is the cell you have held for three rounds and finally labelled |
| C742 | explains functionality | **EARNED** | `bm:91–95` "…if any step fails it just returns an empty dict and whatever called this knows to bail out instead of the simulation crashing" | — the last clause is a genuine *why* (empty dict as the failure signal, crash as the outcome rejected). The first four lines are a *what* walkthrough — at 7–8 the qualifier is "minor issues with clarity", so tighten it. |
| C743 | explains use of data | **SHORT** | `bm:78–80` "mass/velocity/radius come in from whatever called this (usually straight from the sliders), get turned into strings and written out, loading is basically this in reverse" | This says *what*, not *why* — no alternative weighed. That is C733-level. The explain-level version was written at old `bm:88–93` ("unlike slider input, which is already pre-clamped by the slider's own min/max") and deleted this round. `git show 4f77411:Scripts/base_motion.gd` gets it back. |
| C744 | explains use of code structures | **EARNED** | `bm:8–10` "kept all the shared stuff… in this base class so circular_motion.gd doesn't have to repeat it, if I add more simulations later they just inherit from here instead of copy pasting" | — choice plus rejected alternative. The full shape. |
| C745 | all three checks | **EARNED** ⚠ | `bm:73–74` "by the time a typed value actually gets used it's already been through existence, type and range checks, all three every time" — the chain at `bm:66–72`, on live text input | — two *minor inconsistencies* (the 7–8 qualifier): (1) rejection is `push_warning` at `bm:70`, editor console only — the player never learns why their value was ignored; (2) the minimum mass is `0.0` at `bm:17` but `0.01` at `cm:149` and `0.0` again at `cm:97` — three floors, one quantity. And **prove it fires**: type `abc` into the mass box, show the fallback. |
| C751 | applies naming to ALL elements | **SHORT** | `bm:4–6` "naming is consistent everywhere **in here**… same convention as circular_motion.gd not just something specific to this file" | Standard: "open **any file in the project** and the convention holds — variables, constants, functions, classes, nodes, signals, files." Your own comment scopes the claim to two files. Nodes fail (see C731). Files fail: `HomeScreen.gd`, `Global.gd`, `SelectablePanel.gd`, `tempBack.gd`, `switch2.gd` against the snake_case you state and `base_motion.gd`/`circular_motion.gd` follow. |
| C752 | explains ALL, clear and concise | **SHORT** | `bm:143–146` "this file handles saving/loading, validating typed input and pausing/speeding up the sim for every motion simulation that extends it…" | Standard: "**all** data, **all** code structures… and nothing is written twice." Coverage: **29 of feature a's 39 functions carry no comment at all** — 10 of 16 in `bm` (every getter and setter, `get_centripetal_force()`, all four transport functions) and 19 of 23 in `cm`. And this paragraph restates `C744` at `bm:8` — same content, same rejected alternative ("five different scripts") — which is itself the "written twice" failure. |
| C753 | ALL inputs + reasonableness + completeness ✍️ | **SHORT** | `bm:109–111` "checking all 4 columns are actually there before trusting any of them, this is the completeness check on top of the per-value ones below" → `if values.size() < 4:` at `bm:112` | Completeness ✓ (textbook — this is the page's `row.size() < 2` model). All inputs ✓ for the CSV. **Reasonableness is absent** — the page wants "at least one reasonableness rule that **crosses fields**", written down. You have the material: at `MASS_MAX 20` / `RADIUS_MIN 2.5` / `VELOCITY_MAX 60`, which *combinations* produce a force or a period the simulation cannot draw? |

### Feature b — `HomeScreen.gd` (unchanged for three rounds)

| Code | Skill | Verdict | Student's line (file:line) | What's missing |
|---|---|---|---|---|
| C712b | identifies functioning | **EARNED** | `:4–6` "this script controls the simulation selection menu — user picks a category tab, picks a simulation card, then presses start to launch the chosen simulation's scene" | — |
| C713b | identifies input data | **EARNED** | `:55–57` "current_simulation is set from user selection and must be checked against SIMULATIONS before use" | — still the model of specificity your C713 should copy |
| C721b | applies naming to variables | **EARNED** | `:103–104` `var tabs = get_tree().get_nodes_in_group("category_tabs")` — "short but still clearly describes the group of nodes it holds" | — |
| C722b | outlines functioning | **EARNED** ⚠ | `:163–165` "sets the currently selected simulation id, checks it's a real entry, then refreshes the title/description/formula labels to match — nothing else in the UI updates from this function" | — same placement point as C722: this is line 163 of 184, not a header. It outlines one function, not the file. |
| C723b | existence checking | **EARNED** | `:168–169` `if simulation not in SIMULATIONS:` — "confirms the id is a real dictionary key" | — a real check on a user-driven value |
| C724b | type checking | **SHORT** | `:128–129` `if event is InputEventMouseButton:` | Event dispatch, not validation of a data input. (The page does bless `is` — `if child is Card:` — but validation is defined as checking "the reasonableness of **data inputs**"; this is a callback fan-out.) Your real type check is feature a's. Retarget or retire. |
| C725b | range checking | **SHORT** | `:109–111` `if tabs.size() > 0:` — "ensures the tabs list actually has an item before indexing" | A guard on an internal node list is not an input range check — no user value is being tested. Rejected last round on this exact ground. |
| C731b | applies naming to interface controls | **SHORT** | `:71–72` "formula_label clearly names both what it displays and what kind of node it is" | **Reversal — see the section above.** The comment describes a variable name (C721). The scene tree has `Button2`–`Button5`, `Label2`, `Panel2`, `ReferenceRect2`/`3`/`4`, a root Control named `Node2D`, and `start_button: Panel = $ReferenceRect4/Button` (`:73`) — a node called `Button` that is a Panel. |
| C732b | describes functionality | **EARNED** | `:141–143` "looks up the currently selected simulation's data, bails out with a message if nothing was picked, then transitions the whole scene to that simulation's dedicated .tscn file" | — |
| C733b | describes use of data | **EARNED** | `:173–175` "each field pulled from the matched SIMULATIONS entry is written straight into its matching Label, so the on-screen summary always mirrors whatever is stored in the dictionary" | — |
| C734b | evidence of code maintenance | **EARNED** ⚠ | `:153–155` `# TODO: filter simulation list by category` — "tracks known unfinished work so it isn't lost" | — accepted as a *recorded decision*, but this is the weakest form of C734. The page's model is "a comment that could only exist because the code **changed**". Feature a's C734 is the model; `SelectablePanel.gd:49–51` in this same feature holds a real one — see Unclaimed evidence. |
| C735b | two checks working | **SHORT** | `:113–115` | Built from the C725b range check above, which is not an input check. One valid check (C723b) is not two. |
| C741b | applies naming to code structures | **EARNED** | `:156–159` `func change_options(category: String)` — "function name clearly matches its handler role" | — verb phrase, as the page asks |
| C742b | explains functionality | **EARNED** | `:79–81` "…then selects the first tab and first card **so the menu never opens on an empty state**" | — the last clause is the *why*. The weakest of your explains (no alternative weighed) but it clears the bar. |
| C751b | applies naming to ALL elements | **SHORT** | `:7–9` "consistently across ALL elements in this file" | Same evidence as C731b. "ALL" is falsifiable in twenty seconds by opening the scene dock, and it is false. |

### Feature c — `Settings.gd` · `switch.gd` (unchanged for three rounds)

Abbreviations: `S` = `Scripts/Settings.gd`, `sw` = `Scripts/switch.gd`.

| Code | Skill | Verdict | Student's line (file:line) | What's missing |
|---|---|---|---|---|
| C712c | identifies functioning | **EARNED** | `S:4–6` "this script reads/writes the shared Global dark-mode and formula-visibility settings, and toggles the dark-mode button + canvas visibility to match" | — |
| C721c | applies naming to variables | **EARNED** | `sw:15–16` `var current_style` — "names both what it holds and that it reflects the active/current theme" | — but `cooldown` (`S:16`) is a boolean that does not read as a question, and it is untyped |
| C722c | outlines functioning | **EARNED** ⚠ | `S:21–23` "reads the current dark_mode value, then sets both the toggle button's visual state and the CanvasLayer's visibility to match it on scene load" | — same placement point as C722/C722b: inside `_ready()`, not a file header |
| C731c | applies naming to interface controls | **SHORT** | `sw:30–32` "`$Panel` / `$Panel2` / `$Sprite2D` / `$Sprite2D2` names describe exactly which visual layer each node represents" | They don't — `switch.tscn` confirms all four are Godot's defaults, the page's exact anti-pattern. Rejected last round; unchanged. `OnPanel` / `OffPanel` / `IconOn` / `IconOff` flips this in one minute. |
| C733c | describes use of data | **EARNED** | `S:59–61` "flips the shared Global.show_formula flag, which other scenes read to decide whether the formula label should be visible — this script never reads that value itself" | — |
| C734c | evidence of code maintenance | **EARNED** ⚠ | `S:89–92` "TODO: pull 0.3 out into a named constant so the cooldown and the tween durations in Control4.gd can't drift out of sync" | — accepted: it records a real coupling risk you noticed, which is closer to "a lesson learned" than a bare TODO. Still the weak form. |
| C741c | applies naming to code structures | **EARNED** | `sw:25–26` `func on()` / `func off()` — "short but immediately readable given this is a toggle-button script" | — |
| C742c | explains functionality | **EARNED** | `S:69–73` "this is a debounced toggle — cooldown blocks repeat presses while the animation is running… so rapid clicking can't fire the animation multiple times at once" | — still the best single comment in the project |
| C743c | explains use of data | **EARNED** | `sw:43–47` "on() and off() both write literal target positions/visibility flags rather than reading them from data, because there are only ever two fixed visual states — storing them as data instead of two short functions would add complexity without adding flexibility" | — choice plus the alternative rejected, in one sentence. This is what `C743` in feature a needs to look like. |
| C744c | explains use of code structures | **EARNED** | `sw:6–9` "the toggle STATE lives in Settings.gd… while the toggle ANIMATION lives here in the button's own script — keeping 'what the setting is' separate from 'how it looks when it changes' so either half can be edited without touching the other" | — |
| C751c | applies naming to ALL elements | **SHORT** | `S:7–9` "consistently across ALL elements in this pair of files" | `Settings.tscn`: `Control`, `Control2`, `Control3`, `Control4`, `Panel3`–`Panel6`, `Title2`–`Title11`, `Settings2`, and camelCase `speedSlider` / `speedLabel` / `massSlider`. Rejected last round; unchanged. |

---

## Malformed labels — all 21 feature-`a` claims

Not a regression from last round (they were unsuffixed at `4f77411` too, and were read
generously), but they cannot go on counting as `a` cells forever. **Fourteen EARNED
verdicts are riding on these.**

| File | Lines | Reads | Should read |
|---|---|---|---|
| `Scripts/circular_motion.gd` | 3, 6, 15, 27, 51, 92, 162, 191, 262 | `C711`, `C712`, `C721`, `C731`, `C722`, `C732`, `C733`, `C734`, `C713` | `C711a`, `C712a`, `C721a`, `C731a`, `C722a`, `C732a`, `C733a`, `C734a`, `C713a` |
| `Scripts/base_motion.gd` | 4, 8, 25, 53, 57, 63, 67, 73, 78, 91, 109, 143 | `C751`, `C744`, `C741`, `C723`, `C724`, `C725`, `C735`, `C745`, `C743`, `C742`, `C753`, `C752` | append `a` to each |

Find `## C7` in both files, add one character each time. Ten minutes at most, and it is
worth more than any new code you could write in the same ten minutes.

Also worth deleting while you are in there: the mangled fragment at `sw:49`
(`# 4. Animate the panel position smoothly over 0.4 secondsColor(0.165, 0.165, 0.165)`),
still present for the third round. It is noise, and "clear and concise" is the C752
descriptor's own wording.

---

## Unclaimed evidence — label these for free marks

**`Scripts/SelectablePanel.gd` is still completely unlabelled — third round running,
88 lines, zero labels, byte-for-byte unchanged since `4199806`.** It is feature b's only
route above the 5–6 band and it holds four C7 cells already written:

- **`SelectablePanel.gd:29` earns `C751b`** — `signal panel_selected(id: String)`.
  The C751 page's own worked example is *"signal: past tense"* (`result_recorded`,
  `remove_requested`). `panel_selected` is snake_case past tense — you already write
  signals to the convention. It is one of the element kinds C751 wants and you are
  claiming C751b without it. Add
  `# C751b — signal named snake_case past tense, same convention as the vars and funcs`
- **`SelectablePanel.gd:25` earns `C721b`** — `var is_selected: bool = false`.
  The page: *"Every boolean starts `is_` — the name reads as the question the code asks."*
  This is the exact pattern `tween_toggle` and `cooldown` fail. Point C721b here and the
  boolean corner in feature b closes.
- **`SelectablePanel.gd:60`/`:69`/`:74` earn `C741b`** — `select()`, `deselect()`,
  `_apply_style()`: two public verbs plus a private helper, the underscore convention
  applied deliberately. Stronger than `change_options`.
- **`SelectablePanel.gd:49–51` earns `C734b`** —
  `if title and title.label_settings: title.label_settings = title.label_settings.duplicate()`.
  That `duplicate()` could only be there because you hit the bug where every panel shared
  one `LabelSettings` resource and they all recoloured together. Write that sentence and
  you have real maintenance evidence instead of a TODO.

**Feature b has no C744 claim at all, and one is already written:**

- **`HomeScreen.gd:12` earns `C744b`** — *"Add or edit simulations here — no other code
  needs to change."* That is an explain-level sentence about a code structure: the whole
  point of the `SIMULATIONS` dictionary is that the UI is data-driven. Expand it by one
  clause naming the alternative ("rather than a scene-switch branch per simulation") and
  it is a 7–8 cell in a feature that currently tops out at C742b.

**Feature a — better sites than the ones you labelled:**

- **`base_motion.gd:121–126` earns `C733a`** — the returned Dictionary, four named keys
  (`mass`, `velocity`, `radius`, `time_scale`) built from one CSV row. C733 is anchored on
  *a variable or structure*; this is the structure the whole save/load feature turns on,
  and it carries no comment.
- **`base_motion.gd:96` and `:100–103` earn `C723a`** — `if not FileAccess.file_exists(path)`
  and `if file == null`. Existence checking on the *file* input, not just the text input.
  Naming these makes C713a's "every input" claim true and feeds C753a.
- **`base_motion.gd:116–119` earns `C724a`** — `_is_valid_float_string()` applied to all
  four CSV columns at once. Type checking across a whole record, which is exactly the
  "all relevant inputs" half of C753.
- **`base_motion.gd:17–22` earns `C721a`** — six named range constants in
  `UPPER_SNAKE_CASE`. This is the page's "magic numbers replaced by named constants"
  clause, already satisfied for the bounds — it is only `30.0` that is missing.

**Feature c has zero C7-3 claims — seven empty cells — and the way in is a real bug:**

- `Global.gd:4` declares `var default_speed: int = 1`, but `Settings.gd:52` assigns it a
  float slider value every frame and `circular_motion.gd:58` feeds it to
  `Engine.time_scale`. A 1.5× setting truncates to 1; anything below 1.0 collapses to 0.
  Validating that value on the way in — is it a number, is it inside 0.1–3.0 — earns
  **C724c**, **C725c** and **C735c**, and fixes a live defect at the same time. See
  Top 3, item 3.

---

## Verb-ladder check (C7-2) and combination check (C7-3)

### The ladder — *identifies → outlines → describes → explains*

| Rung | Verb | Claimed | Sound | Assessment |
|---|---|---|---|---|
| C712 (1–2) | **identifies** functioning | a, b, c | **3 / 3** | Above the bar everywhere. |
| C722 (3–4) | **outlines** functioning | a, b, c | **3 / 3** ⚠ | All three outline accurately. None is a *header comment* — see below. |
| C732 / C733 / C734 (5–6) | **describes** | a: 3 · b: 3 · c: 2 | **8 / 8** | The strongest rung you hold. |
| C742 / C743 / C744 (7–8) | **explains** | a: 3 · b: 1 · c: 3 | **6 / 7** | Only `C743` (a) fails — it describes rather than explains. |
| C752 (9–10) | **explains ALL, clear and concise** | a | **0 / 1** | Coverage and duplication, not writing quality. |

**Where you actually sit: solidly at 7–8 on C7-2, blocked from 9–10 by coverage, not by
ability.** Six of your seven explain-level claims are genuine — they name a choice and the
alternative rejected, which is the exact move the page says separates 7–8 from 5–6.
`sw:43` (C743c) and `sw:6` (C744c) would pass at any school. That is not the problem.

The problem is that **29 of the 39 functions in feature a — the feature you claim C752 on
— carry no comment at all**: every getter and setter in `base_motion.gd`,
`get_centripetal_force()`, all four transport functions, and 19 of the 23 functions in
`circular_motion.gd`. C752 asks whether "a stranger could maintain the feature from the
comments alone". Right now a stranger gets ten functions documented and twenty-nine bare.
You do not need better sentences; you need more of the ones you already write well.
(Features b and c are far better covered — 6 of 8 and 8 of 9 — which is why the C7-2
gap sits where it does.)

**One systematic fix, worth three cells at once:** all three C722 claims sit *inside a
function*, not at the top of a file. The page defines a header comment as "meaningful
comments at the top of a source code file — the file's name, purpose, author and date"
and earns the tick when "a reader knows what happens in this file **without scrolling**".
Yours are at line 51 of 274, line 163 of 184, and line 21 of 96. In Part B you write the
documentation from scratch and a marker looking for a header comment looks at line 1.
Move all three to the top and add the filename/author/date line.

### The combination — C735 / C745 / C753

- **C735a / C745a are the real thing, and they are live.** Existence → type → range in
  the one order that cannot crash, in a reusable helper, reached from three
  `text_submitted` connections that a real user keystroke fires. This is the best C7-3
  work in the class and nothing in this report should read as diminishing it.
- **Two minor inconsistencies keep it at 7–8 rather than higher.** The 7–8 qualifier is
  *"minor inconsistencies"*; 9–10 requires *"none"*. Yours are (1) rejection is invisible
  to the player — `push_warning` writes to the editor console, and the page is explicit
  that "error messages are part of the standard"; and (2) the minimum mass is `0.0` at
  `bm:17`, `0.01` at `cm:149` and `0.0` again at `cm:97`. Three floors for one quantity is
  precisely what a consistency qualifier measures.
- **Feature b's combination claims are built from rejected parts.** C725b is a guard on an
  internal node list and C735b is built on top of it. C723b is genuine and stands alone —
  one valid check is C723, not C735. Retire C724b/C725b/C735b rather than defending them;
  feature a already carries the combination codes for you.
- **Feature c has no C7-3 evidence at all** — seven empty cells, and its one real data
  input (the speed slider) is unvalidated *and* wrongly typed.
- **C753 is one sentence away.** You have the completeness check (`values.size() < 4` —
  textbook, matches the page's `row.size() < 2` model) and you have every CSV column
  type-checked and clamped. What is missing is the reasonableness rule that crosses
  fields, written down. Note that C753 is ✍️ — the rule earns nothing until it is prose.

---

## Top 3 next actions (biggest marks for least work)

**1. Add the `a` suffix to 21 labels, and put back the C6 labels you deleted.**
Ten minutes, no thinking, and it is worth more than anything else in this report.
Fourteen EARNED C7 verdicts currently sit on labels that cannot be counted into a cell
(`C744` names no box; `C744a` does) — that is 14 cells recovered for 21 keystrokes. Then
`git show 8d9d1c4:Scripts/base_motion.gd` and `git show 8d9d1c4:Scripts/circular_motion.gd`
and copy the C6 labels back onto the lines they belong to: 20 of your 22 confirmed C6
feature-`a` cells lost their labels in this commit. Between them that is potentially 34
cells across two criteria, and not one line of new code.

**2. Do the scene-tree rename — third time of asking, and it is now blocking six cells,
not four.** `C751a`, `C751b`, `C751c`, `C731a`, `C731b` and `C731c` are all SHORT on the
same evidence: `Button2`–`Button5`, `Panel2`, `Panel3`, `Label2`–`Label4`, `Control`–`Control4`,
`Title2`–`Title11`, `ReferenceRect2`–`ReferenceRect4`, camelCase `massSlider`/`speedLabel`,
lowercase `layout`/`options`/`show`/`hide`/`save`/`import`, `Sprite2D2`, a root Control
named `Node2D`, and a node called `Button` that is a Panel. The C071 page names this exact
list as the anti-pattern. Right-click → Rename in the editor keeps every reference safe;
do it one feature at a time and say so in each commit message — that commit history is
itself naming *and* maintenance evidence. Start with `switch.tscn` (four nodes:
`OnPanel`, `OffPanel`, `IconOn`, `IconOff`) so you can see C731c flip inside five minutes.
While you are there, rename the four non-conforming script files (`HomeScreen.gd` →
`home_screen.gd`, `Global.gd`, `SelectablePanel.gd`, `tempBack.gd`, `switch2.gd`) — files
are one of the element kinds C751 audits.

**3. Write the three sentences C7-2 and C7-3 are missing, then make the validation
visible.** In order of value:
   - **The reasonableness rule (`C753a`, ✍️, 9–10).** Beside the constants at `bm:17–22`,
     one comment: which *combinations* of mass, radius and velocity are individually legal
     but cannot be true together for a simulation you can actually draw? (A 20 kg mass at
     radius 2.5 with velocity 60 gives a force the label can print but a ball the screen
     cannot show.) With the completeness check you already have, that is C753a.
   - **Restore `C743a`'s explanation.** `git show 4f77411:Scripts/base_motion.gd` — the
     "unlike slider input, which is already pre-clamped by the slider's own min/max"
     paragraph is explain-level prose you wrote yourself and deleted this round.
   - **Make rejection visible and prove it fires.** Replace the `push_warning` at `bm:70`
     with a message on a Label — "Enter a number between %s and %s" — then run the test:
     type `abc` into the mass box, screenshot the message. That closes C745's first
     inconsistency and satisfies the page's "then prove it fires".

**After those:** validate the speed slider in feature c (`Global.gd:4` is typed `int` and
`Settings.gd:52` assigns it a float — a 1.5× setting silently truncates to 1). Change it
to `float`, add a type and range check on the way in, and feature c goes from zero C7-3
cells to three. It is also a real bug, which makes it C734 evidence when you write down
what you fixed.

---

## Condensed verdicts

| | Codes |
|---|---|
| **EARNED (33)** | **a (14, all labels missing the `a`):** C711 · C712 · C722⚠ · C723 · C724 · C725 · C732 · C733 · C734⚠ · C735 · C741 · C742 · C744 · C745⚠<br>**b (10):** C712b · C713b · C721b · C722b⚠ · C723b · C732b · C733b · C734b⚠ · C741b · C742b<br>**c (9):** C712c · C721c · C722c⚠ · C733c · C734c⚠ · C741c · C742c · C743c · C744c |
| **SHORT (14)** | **a (7):** C713 — the imported CSV is not named · C721 — `30.0` is still a magic number ×3 · C731 — the scene tree is Godot defaults · C743 — describes, does not explain (the explanation was deleted this round) · C751 — nodes and file names break "ALL" · C752 — 29 of feature a's 39 functions carry no comment; duplicates C744 · C753 — no reasonableness rule written<br>**b (5):** C724b — event dispatch, not input validation · C725b — guard on an internal node list · C731b — scene tree (**reversal**, see above) · C735b — built on the rejected range check · C751b — scene tree contradicts "ALL"<br>**c (2):** C731c — `$Panel`/`$Sprite2D2` are Godot defaults · C751c — `Control4`, `Title11`, camelCase nodes |
| **NOT FOUND (0)** | — |
| **Malformed (21)** | Every feature-`a` label: `cm:3,6,15,27,51,92,162,191,262` and `bm:4,8,25,53,57,63,67,73,78,91,109,143` |
| **Free cells flagged** | **C751b** `SelectablePanel.gd:29` · **C721b** `SelectablePanel.gd:25` · **C741b** `SelectablePanel.gd:60`/`:69`/`:74` · **C734b** `SelectablePanel.gd:49–51` · **C744b** `HomeScreen.gd:12` · **C733a** `bm:121–126` · **C723a** `bm:96`/`:100` · **C724a** `bm:116–119` · **C721a** `bm:17–22` · **C724c/C725c/C735c** the speed slider (`Global.gd:4` + `Settings.gd:52`) |

⚠ = standard met, but a named risk sits on it — read that row's note before validation.

---

*Advisory only — no band here is final. All three C7 indicators are assessed live in
Part B of the single 100-minute Live Coding Validation, on a comment-stripped copy of the
one feature the teacher picks: a naming audit (C7-1), the internal documentation written
from scratch (C7-2), and input validation with a test run showing a check fire (C7-3).
C7-2 is where that bites hardest — every explanation in this report is one you will be
asked to reproduce without notes, in your own words. The six that stand are genuinely
good; make sure they are genuinely yours to say out loud.*
