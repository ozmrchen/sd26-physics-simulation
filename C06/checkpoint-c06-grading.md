# Nathan — C06 checkpoint feedback — 2026-08-12

**Graded at commit `8d9d1c4` ("update") · branch `checkpoint-c06` · date 2026-08-12**

**Policy: accept-all.** This grades the artefact exactly as submitted. Building on the
studied projects and using AI help are both legitimate and neither discounts a verdict.
No ownership or authenticity cap is applied here. Authenticity is tested once, at the
supervised Live Coding Validation, where every claim below has to be defended live.

**Rubric:** the ten reference pages in `C06-Reference-Godot/`. Each code's *"Earns the
tick"* line is the standard; where my verdict differs from the checkpoint-1 round, the
page won and I have said so explicitly.

---

## Score

| | |
|---|---|
| Label claims found | **75** (permissive sweep: `# C6`, `#C6`, `#6`, spaced/unspaced, any suffix) |
| Distinct codes touched | **34 of 34** — every code on the tick sheet is claimed at least once |
| Formal cells claimed | **72 / 102** (codes carrying a proper `a`/`b`/`c` suffix) |
| Malformed labels | **3** — all three are *regressions* from correct labels at the last round |
| **EARNED** | **51** |
| **SHORT** | **24** |
| **NOT FOUND** | **0** — every label points at code that actually exists |

Zero NOT-FOUND is worth saying out loud. Nathan does not claim code he has not written.
Every one of the 24 SHORTs is a real line that falls short of its standard, not a
phantom. That is a much better place to be than the reverse.

**Feature map:** `a` = `circular_motion.gd` + `base_motion.gd` + `Global.gd` ·
`b` = `HomeScreen.gd` · `c` = `Settings.gd` + `switch.gd`

| Feature | Claims | EARNED | SHORT | of which formal cells |
|---|---|---|---|---|
| a — circular motion | 31 | 22 | 9 | 30 (22 E · 8 S) + 1 malformed (S) |
| b — home screen | 22 | 15 | 7 | 20 (13 E · 7 S) + 2 malformed (both E) |
| c — settings | 22 | 14 | 8 | 22 (14 E · 8 S) |
| **Total** | **75** | **51** | **24** | **72 / 102** |

---

## What changed since the last graded round

Checkpoint 1 was graded at `4199806` on branch `checkpoint1` (report:
`C06/checkpoint1-grading.md` on that branch). **Checkpoint 1.1 was never completed** —
branch `checkpoint1.1` carries only the teacher-written `PRACTICE.md` brief for the
stripped-comments practice run; no student labels were ever committed to it. So there is
no 1.1 baseline, and everything below compares `8d9d1c4` against `4199806` directly.
(Branch `checkpoint2` holds a student-made `C07/checkpoint2-grading.md`; ignored, as
instructed.)

**The headline: he built the base class.** The checkpoint-1 report closed with "Beyond
the list — where the top band is genuinely yours to take", and named a `Simulation` base
class as the one design decision that would open 9–10 honestly. He did it. `base_motion.gd`
is new — 177 lines, `class_name BaseMotionSimulation` — and `circular_motion.gd` shrank by
131 lines because the validation battery, the CSV save/load and the transport controls
genuinely **moved out of the subclass and up into the parent**:

```
4199806:circular_motion.gd          8d9d1c4:Scripts/base_motion.gd
  _is_valid_float_string()      →     _is_valid_float_string()
  _clamp_to_range()             →     _clamp_to_range()
  _try_parse_validated_float()  →     try_parse_validated_float()   (underscore dropped
  save_to_csv()                 →     save_state_to_csv()            — now the public
  load_from_csv()               →     load_state_from_csv()          subclass interface)
  (transport, inline)           →     pause/resume/speed_up/slow_down()
```

That is not a comment claiming generalisation. That is generalisation, in the diff. It is
the single strongest thing in this snapshot and it is why C651a, C654a and C655a all stand.

Also addressed from the checkpoint-1 improvement list:

- **Item 2 — `Global.gd` typed, C631/C634 claimed there. Done.** All five autoload vars
  are now typed (`int`, `bool`, `String`, `int`, `bool`) and two carry labels. `Global` is
  a registered autoload (`project.godot [autoload] Global=…`), which the Local-and-Global
  page names as "the Godot-idiomatic project-wide global". The rejected node-reference
  claims at old `circular_motion.gd:41` and the weak script-member claim at old `:75` are
  both gone. This is exactly the repair that was asked for.
- **Item 3 — two of three relabels done.** `C635a` moved off the format-call literal onto
  the real array (`:230`). `C658b` → `C638b` (correct — that reasoning *is* 5–6 band).
  The why-float prose is now labelled `C629a` at `:66`.
- **Item 3, third relabel and the C741a mislabel** — the bogus `C741a - interface controls`
  on the engine-autogenerated handler name is gone. Good.

Not addressed — see **Still outstanding**.

### Where this round grades differently from checkpoint 1

Two reversals the teacher should adjudicate, because both move marks:

1. **C644/C645 on features b and c are downgraded to SHORT (scope).** Checkpoint 1 credited
   "three *distinct* data-source whys (CSV / in-memory const / autoload)" and called it the
   best reasoning in the class. The reasoning *is* good — but the Data Sources page opens
   with "the study design names exactly three data sources: plain text (TXT), delimited
   (CSV) and XML files", and the scope rule is applied mechanically. An in-memory `const`
   Dictionary (b) and an autoload singleton (c) are not files and are not any of the three.
   Only feature a's CSV is in scope. This costs four cells at the 7–8 band. I have flagged
   it as the top repair because it is *cheap* to fix properly — see Top 3, item 1.
2. **C634b is downgraded to SHORT**, consistent with checkpoint 1's own rejection of the
   identical claim at old `circular_motion.gd:41` ("`@onready` node ref is not a global").
   He fixed that one and left this one. The honest C634 site is now `Global.gd:5`, which he
   has labelled correctly.

Moving the *other* way: **C631b and C631c stand this round** where checkpoint 1 marked them
WEAK. The Local-and-Global page's own table defines global as "Outside every function, at
script level | Every function in the script", and its worked example is literally
`extends Control` + a script-level var. I graded to the page. He should still be ready to
say at validation which kind of global he means — script-level or autoload — because the
distinction is exactly what a marker will probe.

---

## Verdicts

### Feature a — `circular_motion.gd` · `base_motion.gd` · `Global.gd`

Abbreviations: `cm` = `Scripts/circular_motion.gd`, `bm` = `Scripts/base_motion.gd`,
`G` = `Scripts/Global.gd`.

| Code | Skill | Verdict | Student's line (file:line) | What's missing |
|---|---|---|---|---|
| C611a | instructions | **EARNED** | `cm:38` `@onready var velocity_slider: HSlider = $layout/options/velocitySlider` | — |
| C612a | arithmetic ops | **EARNED** | `cm:133` `var force: float = (current_mass * linear_velocity * linear_velocity) / current_radius` | — |
| C613a | text data | **EARNED** | `cm:139` `formula_label.text = "F = (%.2f * (%.2f)² ) / %.2f" % [...]` | — |
| C614a | numeric data | **EARNED** | `cm:70` `var radius: float = 100.0` | — |
| C615a | Boolean data | **EARNED** | `cm:73` `var tween_toggle: bool = true` | — |
| C621a | local variables | **EARNED** | `cm:125` `var linear_velocity: float = velocity_slider.value` | — |
| C622a | constants | **EARNED** | `cm:59` `const RADIUS_SCALE := 20.0` — CAPITALS, never changes, used at `:146`/`:176` | — |
| C623a | logical ops | **EARNED** | `cm:178` `if new_radius > 0 and radius > 0:` | — |
| C624a | conditional ops | **EARNED** | `cm:132` `if current_radius > 0:` | — |
| C625a | sequence | **SHORT** | `cm:83–86` — four setup calls | Standard: "a labelled block that **breaks if you swap two lines**." These four are order-independent — `_update_all_labels()` reads the sliders directly, so nothing breaks. |
| C626a | selection | **EARNED** | `cm:132–137` `if current_radius > 0: … else: force_label.text = "= 0.00 N"` — both outcomes reachable | — |
| C627a | GUIs | **EARNED** | `cm:37` `@onready var radius_slider: HSlider = …` into a real Control scene; runtime output at `cm:135` | — |
| C628a | typed locals | **EARNED** | `cm:125` `var linear_velocity: float` (also `cm:195`) | — |
| C629a | why data types | **EARNED** | `cm:66` `# why float: angle/angular_velocity are continuous, not whole-number therefor a float should be used` | — (typo: "therefor") |
| C631a | global variables | **EARNED** | `G:4` `var default_speed: int = 1` — `Global` is a registered autoload | — |
| C632a | iteration | **EARNED** ⚠ | `cm:234` `for i in range(sliders.size()):` | Standard met — but see **New problems**: `_reset_sliders_to_defaults()` now has no caller, so this loop is unreachable. |
| C634a | typed globals | **EARNED** | `G:5` `var show_formula: bool = true # … is a bool as it's either on or off` — type stated + why | — |
| C635a | arrays | **SHORT** | `cm:230` `var sliders: Array = [mass_slider, velocity_slider, radius_slider]` | Standard: "Typed (`Array[float]`) earns it… `Array[int]` (or a comment naming the type) is required." `: Array` names the container, not the element type. Write `Array[HSlider]`. |
| C641a | functions | **SHORT** | `cm:79` `func _ready() -> void:` | Standard: "a function with **parameters and a return value, called from elsewhere in the program**. A `func` that Godot calls for you (`_ready`, `_process`) shows the syntax but not the skill." |
| C642a | methods | **SHORT** | `cm:101` `save_dialog.file_selected.connect(_on_save_file_selected)` | Standard: "a method defined **in your own class** and called **through an object**." The page names `connect()` explicitly as a non-example: "`get_children()`, `connect()` and `change_scene_to_file()` … None earn C642." |
| C643a | access modifiers | **EARNED** | `cm:67` `var _angle: float = 0.0   # C643a - "_" prefix denotes private/internal use` — convention applied consistently (`_angle`, `_angular_velocity`, and `bm:42–45` behind getters) *and named* | — |
| C644a | types + structures + sources | **EARNED** | `bm:143–144` `var values: PackedStringArray = file.get_csv_line()` → `bm:156–161` returns a typed `Dictionary`; write side `bm:128–129` `file.store_csv_line([...])`. CSV source → structure → typed values, all three layers | — |
| C645a | why data sources | **EARNED** | `cm:17–19` "using CSV because it is small, human-readable, and can be opened outside Godot for marking/checking" (also `bm:119`) | — |
| C651a | classes | **EARNED** | `bm:1` `class_name BaseMotionSimulation` — a class he wrote, with fields (`_mass`, `_radius`, `_velocity`, `_cooldown`) and methods | — |
| C652a | objects | **SHORT** | `bm:11–13` "whichever simulation scene uses this class becomes an OBJECT" | Standard: "**`.new()` creating an object your program then uses.**" No `.new()` or `.instantiate()` for this class exists anywhere in his code — the only `.new()` is `StyleBoxFlat.new()` (an engine class). The nearest real creation site is `HomeScreen.gd:150` and it is unlabelled. |
| C653a | abstraction | **SHORT** | `bm:81` `func get_centripetal_force() -> float:` | Standard: "a base class **used** through its simple interface while the details stay inside." It is never called — `cm:133` re-computes `(m·v²)/r` inline, duplicating the exact formula the base class exists to hide. |
| C654a | encapsulation | **EARNED** ⚠ | `bm:42` `var _mass: float = 1.0` behind `bm:61` `func set_mass(value: float) -> void: _mass = clamp(value, MASS_MIN, MASS_MAX)` — private data, changed only through a validating method | — (but see **New problems**: this state is currently dead) |
| C655a | generalisation | **EARNED** | `bm:15–22` + the diff above — validation, CSV persistence and transport controls genuinely lifted out of `circular_motion.gd` into one parent. "Point at the duplication you avoided" — he can point at the diff | — |
| C657a | range of types/structures/sources | **SHORT** | `bm:24–27` "float …, bool …, String …, const …, and a CSV file on disk as the data source" | Standard: "several types, several structures, **several sources**, each doing a job the others could not." He has exactly **one** source (CSV) and his own comment says so. Also: `const` is a storage class, not a data type. |
| C658a | why all of them | **SHORT** | `bm:29–34` "a base class was chosen over five unrelated scripts because…" | Standard: explains why the selected **data types, structures and sources** were used, weighing alternatives ("CSV over XML because…"). This paragraph weighs base-class vs five scripts — that is code structure (C744), not data. Same category error checkpoint 1 rejected at `switch.gd:2`. |
| C656 *(malformed — no suffix)* | inheritance | **SHORT** | `cm:1–2` `extends BaseMotionSimulation` / `# C656 - Inheritance` | The parent is now genuinely his own class — real progress. Standard still wants: "`super` used, at least one method overridden." `super` appears **nowhere** in the project, and `circular_motion.gd` overrides no method of `BaseMotionSimulation`. |

### Feature b — `HomeScreen.gd`

| Code | Skill | Verdict | Student's line (file:line) | What's missing |
|---|---|---|---|---|
| C613b | text data | **EARNED** | `:23` `"description": "An object moving in a circle experiences a centripetal force…"` | — |
| C615b | Boolean data | **EARNED** | `:83` `if Global.dark_mode == true:` (`Global.gd:8` is `var dark_mode: bool = true`) | — (thinnest form — see note under Still outstanding) |
| C621b | local variables | **EARNED** | `:101` `var tabs = get_tree().get_nodes_in_group("category_tabs")` inside `_ready()` | — |
| C622b | constants | **EARNED** | `:61` `const HOME = preload("res://Scenes/HomeScreen.tscn")` — const, CAPITALS, cannot change | — |
| C623b | logical ops | **EARNED** | `:130` `if event.button_index == MOUSE_BUTTON_LEFT and event.pressed:` | — |
| C626b | selection | **EARNED** | `:109` `if tabs.size() > 0: tabs[0].select()` — guard clause, which the page accepts as C626 evidence | — |
| C627b | GUIs | **EARNED** | `:68` `@onready var description_label: Label = $ReferenceRect4/Description`; runtime output at `:178` | — |
| C628b | typed locals | **EARNED** | `:123` `func _on_simulation_selected(id: String) -> void:` — parameters are local, type stated, suits a Dictionary key | — |
| C629b | why data types | **EARNED** | `:53–54` "String is used (not an int/enum id) because the value is used directly as a Dictionary key" — outlines *and* weighs an alternative | — |
| C632b | iteration | **EARNED** | `:92` `for node in get_tree().get_nodes_in_group("category_tabs"):` — over real data, terminates | — |
| C633b | relevant GUI controls | **EARNED** | signal connected in code `:89` `$ReferenceRect4/Button.gui_input.connect(_on_start_gui_input)`; custom signal `:94` `node.panel_selected.connect(_on_category_selected)`; property at runtime `:177` `title_label.text = data["title"]` | — but the *labelled* line (`:73` `start_button: Panel`) is the weakest of the three ingredients: an action belongs on a `Button` per the page's table, and the click is hand-rolled at `:127–132`. Move the label to `:89`. |
| C634b | typed globals | **SHORT** | `:66` `@onready var title_label : Label = $ReferenceRect4/Title` | C634 is about a variable whose data type suits the **data** it holds. An `@onready` node reference is the interface handle — that is C627/C633 evidence, which he already claims at `:69`/`:74`. Checkpoint 1 rejected the identical claim at old `circular_motion.gd:41`. The honest site is `Global.gd:8`. |
| C635b | arrays | **SHORT** | `:105` `var cards = get_tree().get_nodes_in_group("sim_cards")` | Untyped. Standard: "`Array[int]` (or a comment naming the type) is required." Naming "Array" names the container, not the element type — write `var cards: Array[Node]`. |
| C638b | why types + structures | **EARNED** | `:13–16` "a single Dictionary of Dictionaries was chosen … because every simulation needs the same fields … instead of five sets of separate variables" — identifies, at 5–6 band | — |
| C641b | functions | **SHORT** | `:77` `func _ready() -> void:` | Same as C641a: Godot calls it. Nearest real site is `:166` `func change_summary(simulation: String) -> void:` — typed parameter, called from `:125` — but it still needs a return value that gets used. |
| C642b | methods | **SHORT** | `:89` `…gui_input.connect(_on_start_gui_input)` | `connect()` is an engine method — explicit non-example. He has a perfect C642 sitting three lines below at `:109` and does not claim it. See Unclaimed evidence. |
| C643b | access modifiers | **EARNED** | `:134` `func _on_start_pressed() -> void:` + `:135–137` naming the convention. Hand-written and hand-called from `:132` — matches the page's `_open()` / `_on_back_pressed()` precedent | — |
| C644b | types + structures + sources | **SHORT** | `:144` `var data = SIMULATIONS.get(current_simulation, null)` | The **source** layer is absent. C644 is a file (TXT/CSV/XML) parsed into a structure of typed values; an in-memory `const` Dictionary is a structure with no source behind it. |
| C645b | why data sources | **SHORT** | `:138–140` "SIMULATIONS (an in-memory constant) is used as the data source here instead of reading a file" | Scope. "The study design names exactly three data sources: plain text (TXT), delimited (CSV) and XML files." An in-memory constant is not one of them, however well the choice is argued. |
| C656b | inheritance | **SHORT** | `:1–2` `extends Control` / "this script extends the built-in Control class" | Standard: "`extends` on a class of your own." The page's explicit non-example. Rejected at checkpoint 1, unchanged. |
| C636 *(malformed — no suffix)* | records | **EARNED** | `:17` `const SIMULATIONS: Dictionary = {…}` with `:20–26` `"title"/"description"/"formula"/"scene"` — one key per field, an array-of-records equivalent | — (label must read `C636b`) |
| C631b *(malformed — reads `# 1b`)* | global variables | **EARNED** | `:58` `var current_simulation: String = ""` — script level, visible to every func in the script | — (label must read `C631b`) |

### Feature c — `Settings.gd` · `switch.gd`

Abbreviations: `S` = `Scripts/Settings.gd`, `sw` = `Scripts/switch.gd`.

| Code | Skill | Verdict | Student's line (file:line) | What's missing |
|---|---|---|---|---|
| C611c | instructions | **EARNED** | `S:78` `Global.dark_mode = not Global.dark_mode` | — |
| C613c | text data | **EARNED** | `S:55` `get_tree().change_scene_to_file("res://Scenes/HomeScreen.tscn")` | — |
| C614c | numeric data | **EARNED** | `S:87` `await get_tree().create_timer(0.3).timeout` — float literal, type named in the comment | — (thinnest form) |
| C615c | Boolean data | **EARNED** | `S:34` `if Global.dark_mode == true:` | — (thinnest form; `== true` is redundant) |
| C621c | local variables | **EARNED** | `sw:27` `var tween = create_tween().set_parallel(true)` inside `func on()` | — |
| C622c | constants | **EARNED** | `S:11` `const HOME = preload("res://Scenes/HomeScreen.tscn")` | — |
| C623c | logical ops | **SHORT** | `S:62` `Global.show_formula = not Global.show_formula` | Standard: "a condition that **combines** expressions." This is an assignment, and `not` negates a single value rather than combining two. The page's own example is `if not card_registered or balance < 0.0:`. Feature c has no combining condition anywhere. |
| C624c | conditional ops | **EARNED** | `S:74` `if cooldown == false:` | — |
| C625c | sequence | **EARNED** | `S:76–93` `cooldown = true` → flip `dark_mode` → update icon → toggle canvas → `await` → `cooldown = false`. Genuinely breaks if you swap: releasing the cooldown before the await destroys the debounce | — |
| C626c | selection | **EARNED** | `S:34–44` `if Global.dark_mode == true: … else: …`, both outcomes reachable | — |
| C627c | GUIs | **EARNED** | `S:32` `$ReferenceRect4/Panel4/speedSlider.value = Global.default_speed`, `S:50` `speedLabel.text = "%sx" % …` — a working Control-node interface | — but the *labelled* line (`S:44`, a `CanvasLayer`) is not a Control node. Move the label to `S:32`. |
| C629c | why data types | **EARNED** | `S:14–15` "Boolean is used (not e.g. a timer/int) because 'cooldown' only ever needs to represent two states" — outlines and weighs an alternative | — |
| C631c | global variables | **EARNED** | `S:16` `var cooldown = false` — script level, visible to every func in the script | — |
| C633c | relevant GUI controls | **EARNED** | property at runtime `S:50` `speedLabel.text = "%sx" % $…/speedSlider.value` and `S:85` `$CanvasLayer.visible = not $CanvasLayer.visible`; signals connected in the editor (`Settings.tscn`), which the page accepts | — |
| C638c | why types + structures | **SHORT** | `sw:11–13` "the fetched StyleBox is stored in a variable so the theme lookup only has to run once" | C638 identifies why the selected **data types and structures** were used. This is a performance/caching rationale, not a structure rationale. Flagged WEAK at checkpoint 1, unchanged. |
| C641c | functions | **SHORT** | `S:19` `func _ready() -> void:` | Same as C641a/b. Godot calls it. |
| C642c | methods | **EARNED** | `S:36` `$ReferenceRect4/Control4.on()` — `on()` is defined at `sw:24` in a script he wrote, called through a node object. **The strongest C642 in the project** | — |
| C643c | access modifiers | **SHORT** | `S:65` `func _on_control_4_button_up() -> void:` | Standard: "the convention applied consistently *and named*." This handler name is autogenerated by the editor's signal dialog — the underscore was not a choice he made. Flagged at checkpoint 1, unchanged. The honest sites are `SelectablePanel.gd:26`/`:74`. |
| C644c | types + structures + sources | **SHORT** | `S:24` `print(Global.dark_mode)` with `:25–27` naming the autoload as the source | Scope. An autoload singleton is not TXT, CSV or XML — it is not a data source in VCE terms at all. |
| C645c | why data sources | **SHORT** | `S:28–30` "an autoload singleton was chosen over passing values scene-to-scene…" | Scope, same as C644c. Good reasoning about the wrong kind of thing. |
| C656c | inheritance | **SHORT** | `S:1–2` `extends Control` | "`extends` on a class of your own." Rejected at checkpoint 1, unchanged. |
| C658c | why all of them | **SHORT** | `sw:2–5` "on()/off() are split into two separate public methods rather than one method with a boolean parameter…" | C658 explains why the selected data **types, structures and sources** were chosen, weighing alternatives. This is code structure — which he correctly claims as C744c six lines below. Rejected at checkpoint 1 for this exact reason, unchanged. |

---

## Malformed labels — all three are regressions

A marker's eye and any grep will miss these. All three were **correct at `4199806`** and
were broken by an edit in this round — most likely a botched find-and-replace.

| File:line | Now reads | Was at `4199806` | Should read |
|---|---|---|---|
| `circular_motion.gd:2` | `# C656 - Inheritance` | `# C656a - Inheritance: …` | `# C656a` |
| `HomeScreen.gd:18` | `# C636 - Dictionary/record: …` | `# C636b - Dictionary/record: …` | `# C636b` |
| `HomeScreen.gd:59` | `# 1b - Global variable` | `# C631b - Global variable` | `# C631b` |

All three have been assessed on their evidence above (two EARNED, one SHORT). But an
unsuffixed or mangled label cannot be counted into an `a`/`b`/`c` cell as written. Three
keystrokes each.

---

## Unclaimed evidence — label these for free marks

Ranked by what they are worth. Every one is code he has already written.

**At the 9–10 frontier (feature b has no 9–10 cells at all, and this is the way in):**

- `Scripts/SelectablePanel.gd:29` earns **C633b** — `signal panel_selected(id: String)`.
  The GUI page: "Custom signals count as **strong C633 evidence**." Add
  `# C633b — custom signal: a panel reports its own id, so the menu doesn't poll every node`
- `Scripts/SelectablePanel.gd:26` + `:74` earn **C643b / C654b** — `var _style : StyleBoxFlat`
  and `func _apply_style(selected: bool)`: private data plus a private helper, reachable only
  through the public `select()` / `deselect()`. This is the page's encapsulation pattern
  almost exactly. Add `# C654b — private style state, changed only through select()/deselect()`
- `Scripts/SelectablePanel.gd:60`/`:69` earn **C653b** — `func select()` / `func deselect()`:
  `HomeScreen.gd` calls them and never touches `_style`. The sentence to say is "the home
  screen calls `select()` and doesn't care how a panel repaints itself".
- `Scripts/SelectablePanel.gd:22`/`:33` earn **C655b** — `@export var selection_group: String`
  + `add_to_group(selection_group)`: **one class serves both the category tabs and the
  simulation cards.** That is generalisation, in feature b, already built.

**Cells currently SHORT that other lines would repair outright:**

- `Scripts/HomeScreen.gd:109` earns **C642b** — `tabs[0].select()`. `select()` is defined at
  `SelectablePanel.gd:60` in a class he wrote, called through a node object. This is the
  textbook C642 and it is three lines below the `connect()` he labelled instead.
  Add `# C642b — method: select() is mine, called on a panel object`
- `Scripts/base_motion.gd:110` earns **C641a** —
  `func try_parse_validated_float(text: String, min_value: float, max_value: float, fallback: float) -> float:`
  Typed parameters, typed return, called from `circular_motion.gd:310`, `:313` and `:316`
  with the result assigned and used. Every ingredient the standard names.
  Add `# C641a — function: typed parameters and a float return, called from three handlers`
- `Scripts/base_motion.gd:133` earns **C641a** (alternative) —
  `func load_state_from_csv(path: String) -> Dictionary:` called at `circular_motion.gd:205`,
  result used at `:208–211`.
- `Scripts/circular_motion.gd:310` earns **C642a** — `try_parse_validated_float(...)` is his
  own base-class method invoked on this simulation object. Say "inherited from
  `BaseMotionSimulation`, called on this object" so it reads as a method, not a bare function.
- `Scripts/circular_motion.gd:204–213` earns **C625a** — `_on_import_file_selected()`: load
  the CSV → guard on empty → set the three sliders → `_update_values_from_sliders()` →
  `_update_all_labels()`. This one **does** break if you swap two lines. Move the label here.
- `Scripts/Global.gd:8` earns **C631b / C634b** — `var dark_mode: bool = true`, read by
  `HomeScreen.gd:83`, `Settings.gd:34` and `circular_motion.gd:89`. The autoload serves all
  three features; labelling only the `a` cells leaves b and c on weaker sites.
  Add `# C634b — bool: dark mode is on or off, nothing in between`
- `Scripts/Global.gd:6` earns **C631c / C634c** — `var rounding_mode: String = "dp"`.
- `Scripts/base_motion.gd:151–152` earns **C623a** (a much stronger site) —
  `if not (_is_valid_float_string(values[0]) and _is_valid_float_string(values[1]) and …)`
  — a real condition combining four expressions with `and` and `not`.

**Cells not claimed at all in a feature that could carry them:**

- `Scripts/circular_motion.gd:101`, `:110–112`, `:116–118` earn **C633a** — this cell was
  claimed at checkpoint 1 (old `:34`) and has been **dropped**. Feature a has the richest
  interface in the project: HSliders for continuous quantities, LineEdits for typed entry,
  Labels for read-only output, three signals connected in code (`:110–112`) and properties
  written at runtime (`:116–118`). All three C633 ingredients, unlabelled.
- `Scripts/base_motion.gd:156–161` earns **C636a** — the returned Dictionary is a record:
  four named fields (`mass`, `velocity`, `radius`, `time_scale`) built from one CSV row.
  Feature a has no C636 claim.
- `Scripts/base_motion.gd:49–54` earns **C622a** (stronger than `RADIUS_SCALE`) — six named
  range constants, used at `:62`, `:68`, `:74` and `:157–160`.
- `Scripts/base_motion.gd:143–144` earns **C628a** — `var _headers: PackedStringArray` /
  `var values: PackedStringArray`: typed locals of a non-primitive type.
- `Scripts/switch2.gd` is 40 lines with **zero labels** — `:16–40` is a chained selection with
  both outcomes reachable (**C626c**), `:16` `if selected == "dp":` is **C624c**, `:3`
  `var selected = "dp"` is **C613c / C631c**.

---

## Reasoning check — the verb ladder (C629 / C638 / C645 / C658)

| Rung | Verb | Claimed | Sound | Assessment |
|---|---|---|---|---|
| C629 (3–4) | **outlines** why data types | a, b, c | **3 / 3** | Above the bar on all three. |
| C638 (5–6) | **identifies** why types *and structures* | b, c | **1 / 2** | b sound; c is a caching rationale. |
| C645 (7–8) | **describes** why data sources | a, b, c | **1 / 3** | Only a is a VCE data source. |
| C658 (9–10) | **explains** why all of them | a, c | **0 / 2** | Both argue code structure, not data. |

**C629 — all three EARNED, and two of them punch above the rung.** The standard at 3–4 is
merely to *outline* — a phrase. `HomeScreen.gd:53` ("String … not an int/enum id … because
the value is used directly as a Dictionary key") and `Settings.gd:14` ("Boolean … not e.g. a
timer/int … only ever needs to represent two states") both name the alternative they rejected.
That is the *shape* C658 wants, applied to the wrong rung. He already knows how to write the
top-band sentence; he is spending it on the bottom-band code.

**C638b EARNED, C638c SHORT.** `HomeScreen.gd:13` identifies the structure choice and the
alternative rejected ("instead of five sets of separate variables") — that is a clean 5–6.
`switch.gd:11` explains why a lookup is cached, which is about performance, not about why a
data structure was selected. Checkpoint 1 said this; it is unchanged.

**C645a EARNED — still the best data reasoning he has.** "CSV because it is small,
human-readable, and can be opened outside Godot for marking/checking" describes the choice in
a sentence, on a source that is actually in scope. C645b and C645c are equally well written
and describe things that are not data sources.

**C658 earns nothing, twice.** Both `base_motion.gd:29` and `switch.gd:2` argue about how the
*code* is organised. C658 is about data. The gap is small and he is very close to closing it:
he already has the raw material at `circular_motion.gd:14–19`. Turning it into C658 means
adding the alternatives — something like:

```gdscript
# C658a — CSV for the saved session: four values in one flat row, and it opens in
# Excel so a marker can check it without Godot. XML would wrap every number in a tag
# pair for no gain, since there is nothing nested here. TXT would need me to invent
# and parse my own separator — get_csv_line() already does that correctly.
```

That is the whole difference between the 7–8 rung he holds and the 9–10 rung he is claiming.

---

## Still outstanding

From the checkpoint-1 improvement list, unaddressed at `8d9d1c4`:

1. **`SelectablePanel.gd` is still completely unlabelled — 88 bytes-for-byte unchanged
   lines, zero labels, second round running.** It was item #1 on the last list. It is the
   only genuine OOP outside the new base class, and it is the only route into the 9–10 band
   for **feature b**, which currently has none. Four cells (C633b, C643b/C654b, C653b, C655b)
   are sitting there already written. `git diff` confirms the file was not touched this round.
2. **C656 is still claimed on `extends Control` in features b and c** (`HomeScreen.gd:2`,
   `Settings.gd:2`) — the page's explicit non-example, rejected at checkpoint 1. The last
   report warned: "Claiming it on `extends Control` at validation would cost you more than
   the code is worth." Feature a now has a real parent class, so he has earned the right to
   drop the other two without losing anything he cares about.
3. **`switch.gd` is byte-for-byte unchanged** — so C658c (code-structure rationale, rejected)
   and C638c (caching rationale, weak) both stand exactly as they were, and the mangled
   comment fragment at `sw:49` is still there:
   `# 4. Animate the panel position smoothly over 0.4 secondsColor(0.165, 0.165, 0.165)`.
   Deleting it is itself C734 evidence.
4. **The doubled `if Global.dark_mode` blocks in `Settings.gd` were not merged** — still two
   consecutive tests at `:34` and `:41`. Same note: finding and merging it is C734 evidence.
5. **No "why these bounds" sentence.** Item 6 asked why `RADIUS_MIN 2.5` etc. are physically
   sensible. `base_motion.gd:47–48` explains why the *type* is float — good, but a different
   question. What breaks outside those bounds? One sentence turns range checking into
   *reasonableness* checking.
6. **The bare-literal 1–2 band sites persist** (`HomeScreen.gd:84`, `Settings.gd:35`,
   `Settings.gd:88`). These stand under the page's wording and I have marked them EARNED —
   but at the Live Coding Validation a marker will ask him to point at a *declared, typed*
   example. He has one for feature a (`circular_motion.gd:73`) and none for b or c.
   `Global.gd:8` would fix both in one line.

---

## New problems this round

1. **A live button now calls a method that no longer exists — and it strands two cells in
   unreachable code.** `Scenes/CircularMotion.tscn:890` still carries
   `[connection signal="button_down" from="layout/options2/Button2" to="." method="_on_button_2_button_down"]`,
   but `_on_button_2_button_down()` was deleted from `circular_motion.gd` this round. It
   existed at `4199806:314` and called `_reset_sliders_to_defaults()`. Consequences:
   - Pressing the reset button raises a missing-method error at runtime.
   - `_reset_sliders_to_defaults()` (`cm:229`) now has **no caller anywhere** in scripts or
     scenes — so **C632a** (`cm:234`) and **C635a** (`cm:230`), his only iteration and only
     array in feature a, both live in code that cannot run. He will be asked to demonstrate
     them live.

   Restoring the four-line handler fixes the button and both cells at once.

2. **Three previously-correct labels were broken this round** (`C656a`→`C656`,
   `C636b`→`C636`, `C631b`→`1b`). See the malformed table above. Worth understanding *how*
   it happened before it happens again the night before validation.

3. **The encapsulated state in `base_motion.gd` is entirely dead.** `get_mass`, `set_mass`,
   `get_radius`, `set_radius`, `get_velocity`, `set_velocity`, `get_centripetal_force` and
   `resume_simulation` are each referenced exactly once in the whole project — at their own
   `func` line. Meanwhile `circular_motion.gd:70–71` keeps its own *public* `mass` and
   `radius` fields and reads the sliders directly. So the class encapsulates state that
   nothing uses, while the state that is actually used is not encapsulated. C654a stands on
   the page's literal wording, but "show me `set_mass(-50)` being rejected in the running
   program" is a fair validation question and right now the answer is "it never runs".

4. **`Global.default_speed` is typed `int` but is assigned a float every frame.**
   `Global.gd:4` declares `var default_speed: int = 1`; `Settings.gd:52` runs
   `Global.default_speed = $ReferenceRect4/Panel4/speedSlider.value` inside `_process`, and
   `circular_motion.gd:87` feeds it to `Engine.time_scale`. A 1.5× setting truncates to 1 and
   anything below 1.0 collapses to 0. This is new code this round, and it is precisely the
   C634 question — *does the type suit the data?* — answered wrongly. `: float` fixes it.

5. **C633a was dropped.** It was credited at checkpoint 1 (old `:34`, `# C627a and C633a`) and
   the new header keeps only `C627a`. Feature a has the strongest GUI evidence in the project
   and now claims nothing for it. Pure loss, one comment to recover.

---

## Condensed verdicts

| | Codes |
|---|---|
| **EARNED (51)** | **a (22):** C611a · C612a · C613a · C614a · C615a · C621a · C622a · C623a · C624a · C626a · C627a · C628a · C629a · C631a · C632a⚠ · C634a · C643a · C644a · C645a · C651a · C654a⚠ · C655a<br>**b (15):** C613b · C615b · C621b · C622b · C623b · C626b · C627b · C628b · C629b · C632b · C633b · C638b · C643b · *C636b* · *C631b*<br>**c (14):** C611c · C613c · C614c · C615c · C621c · C622c · C624c · C625c · C626c · C627c · C629c · C631c · C633c · C642c |
| **SHORT (24)** | C625a — the four setup calls are order-independent · C635a — needs `Array[HSlider]`, not bare `: Array` · C641a — labelled on `_ready()`, which Godot calls · C642a — `connect()` is an engine method · C652a — no `.new()`/`.instantiate()` anywhere for this class · C653a — `get_centripetal_force()` is never called; `cm:133` re-computes it inline · C657a — one source, not several · C658a — argues code structure, not data · C656(a) — no `super`, no override · C634b — an `@onready` node reference is not a data global · C635b — untyped array · C641b — labelled on `_ready()` · C642b — `connect()` is an engine method · C644b — no source layer behind the structure · C645b — an in-memory `const` is not a VCE data source · C656b — `extends Control` is engine inheritance · C623c — `not` in an assignment does not combine expressions · C638c — a caching rationale, not a structure rationale · C641c — labelled on `_ready()` · C643c — the underscore is on an editor-autogenerated handler name · C644c — an autoload is not a VCE data source · C645c — same scope failure · C656c — `extends Control` · C658c — argues code structure, not data |
| **NOT FOUND (0)** | — |
| **Free marks flagged (unclaimed evidence)** | **C633a** `cm:101`, `:110–112`, `:116–118` *(dropped this round)* · **C636a** `bm:156–161` · **C641a** `bm:110` and `bm:133` · **C642a** `cm:310` · **C625a** `cm:204–213` · **C623a** `bm:151` · **C622a** `bm:49–54` · **C628a** `bm:143–144` · **C642b** `HomeScreen.gd:109` · **C633b** `SelectablePanel.gd:29` · **C643b/C654b** `SelectablePanel.gd:26`+`:74` · **C653b** `SelectablePanel.gd:60`/`:69` · **C655b** `SelectablePanel.gd:22`/`:33` · **C631b/C634b** `Global.gd:8` · **C631c/C634c** `Global.gd:6` · **C624c/C626c/C613c** `switch2.gd:3`, `:16–40` |
| **Malformed (3)** | `cm:2` `# C656` → `# C656a` · `HomeScreen.gd:18` `# C636` → `# C636b` · `HomeScreen.gd:59` `# 1b` → `# C631b` — all three regressions from correct labels at `4199806` |

⚠ = standard met, but see **New problems** (C632a is unreachable; C654a's state is dead).

---

## Do these first — ten minutes, no design decisions

Not marks-ranked, just cheap and currently bleeding:

- Fix the three malformed labels (three keystrokes each).
- Restore `_on_button_2_button_down()` so `CircularMotion.tscn:890` stops calling a missing
  method and C632a / C635a become reachable again.
- `Global.gd:4` → `var default_speed: float = 1.0`.
- Re-add `C633a` to the GUI header in `circular_motion.gd`.
- Delete the mangled fragment at `switch.gd:49`; merge the doubled `if` at `Settings.gd:34/41`.

## Top 3 next actions (biggest marks for least work)

1. **Give features b and c a real data source — save the settings to a TXT file.**
   Roughly fifteen lines in `Settings.gd`: write `Global.dark_mode`, `show_formula`,
   `rounding_mode` and `decimal_places` to `user://settings.txt` on change, read them back in
   `_ready()`. This single addition repairs **C644c** and **C645c** (a source that is actually
   in scope), turns **C657a** from SHORT to EARNED (two sources, each doing a job the other
   could not — a flat CSV table for the simulation session, one-value-per-line TXT for
   settings), and hands him the alternatives-weighing material **C658a** needs. Four cells at
   the 7–8 and 9–10 bands, and it is a feature his SRS wants anyway — settings that survive a
   restart. Highest value in the report.

2. **Wire the base class into its subclass — `get_centripetal_force()`, `super`, one override.**
   In `circular_motion.gd:132–137`, stop re-computing `(m·v²)/r` inline and call the base
   method instead; then add a real override that extends it, e.g.
   `func get_centripetal_force() -> float: return super.get_centripetal_force()` with the
   subclass supplying its own values first. That one edit earns **C653a** (the base finally
   *used* through its interface), **C656a** (his own parent + `super` + an override — the full
   standard, in feature a, honestly), and makes **C652a** demonstrable by labelling
   `HomeScreen.gd:150` as the line that creates the object. Three 9–10 cells from about six
   lines. It also removes the awkward fact that the class exists but nothing calls it.

3. **Label `SelectablePanel.gd` — second round of asking, and it is feature b's only way into
   the top band.** Zero labels, unchanged since `4199806`. Four cells are already written and
   waiting: the custom signal at `:29` (**C633b**), `_style` + `_apply_style()` behind the
   public `select()`/`deselect()` at `:26`/`:74`/`:60`/`:69` (**C643b**, **C654b**, **C653b**),
   and one class serving both tabs and cards via `selection_group` at `:22`/`:33` (**C655b**).
   Feature b currently has no 9–10 cell at all. No new code — just the labels, each with the
   one-sentence *why* the 7–8 and 9–10 bands require.

**After those:** drop `C656b` and `C656c` from `extends Control`. Once item 2 lands, feature a
holds the real inheritance claim and the other two are pure downside at validation.

---

*Advisory only — marks are awarded at the Live Coding Validation.*
